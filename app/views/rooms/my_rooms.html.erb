<%= provide(:title, 'Meus Quartos') %>

<%= render 'home/search' %>

<h1 class="title">
	<%= t ('.title') %>
	<span class="btn btn-alert btn-small"><%= total_user_rooms %></span>
</h1>

<%= render @my_rooms %>