<h1><%= t '.title' %></h1>

<% @user.each do |user| %>
<ul>
	<li><%= link_to "#{user.full_name} - #{user.location} (#{user.email})", user_path(user) %></li>
</ul>
<% end %>

<%= link_to 'Criar novo', new_user_path %>